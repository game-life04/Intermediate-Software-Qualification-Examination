四种码的区别
| 原码(Sign-Magnitude) |   规则   |               |
| :----------------: | :----: | ------------- |
|         正数         |  符号位0  | +0=00000000   |
|         负数         |  符号位1  | −0 = 10000000 |
|                    | 数值部分相同 |               |

| 反码（Ones' Complement） |  规则  |               |
| :------------------: | :--: | :-----------: |
|          正数          | 同原码  |  +0=00000000  |
|          负数          | 按位取反 | −0 = 11111111 |


| 补码（Two's Complement） | 规则   |                                      |
| -------------------- | ---- | ------------------------------------ |
| 正数                   | 正数不变 | +0=00000000                          |
| 负数                   | 反码+1 | −0 → 反码 11111111 + 1 → 溢出 → 00000000 |


| 移码（Bias / Exponent Code） | 规则       |
| ------------------------ | -------- |
| 原反有双零，补移零唯一              | 整体加一个偏移量 |
|                          | 0只有一种表示  |

硬盘容量计算
容量 =  
（记录面数 ×  [(外半径 − 内半径) × 磁道密度] ×  每道扇区数 ×  每扇区字节数)/2 * 1024* 1024（MB）

tip：注意内容

| ❗ 用直径差（错）    | ✅ 必须用半径差       |
| ------------ | -------------- |
| ❗ 忘记 cm → mm | ✅ 一定换 mm       |
| ❗ 忘记转 MB     | ✅ 最后必须 ÷ 1024² |
| ❗ 把盘片数当记录面   | ✅ 本题已给 8 个面    |

数据库故障类型
死锁——>事务故障
死锁的特点
1：多个事务互相等待
2：属于事务执行过程中的问题
3：系统本身没崩
本质：事务级别的问题
考试关键词：回滚某些事务即可恢复


系统断点——>系统故障
系统断点通常指：
- 操作系统崩溃
- 数据库系统崩溃
- 突然停机
- 断电
特点：**整个系统运行被中断**
恢复方式：重启 + 日志恢复

计算机基础杂项
### 1：处理机主要由处理器、存储器和总线组成
其中总线包括：数据总线、地址总线、控制总线

### 2：吞吐率的计算
(1)流水线问题
1.1先清理流水线的瓶颈段以及完成n条指令的总时间。
根据你的描述，流水线的各段耗时如下：
- $S_1 = \Delta t$
- $S_2 = 3\Delta t$ （**瓶颈段**，耗时最长）
- $S_3 = \Delta t$
- $S_4 = 2\Delta t$
- $S_5 = \Delta t$
在流水线处理中，**流水线周期 $\Delta T$** 取决于最慢的那一段。因此：

$$\Delta T = \max(S_1, S_2, S_3, S_4, S_5) = 3\Delta t$$

1.2计算执行n条指令的总时间T(n)
流水线执行n条指令的时间公式为：
**总时间=第一条指令执行完的时间+剩余(n-1)条指令各花费一个周期的时间**

**1.第一条指令所需时间(建立时间)**
$T_{first} = \Delta t + 3\Delta t + \Delta t + 2\Delta t + \Delta t = 8\Delta t$

2 **剩余 $(n-1)$ 条指令所需时间**：
$T_{remaining} = (n-1) \times 3\Delta t$
**总时间 $T_n$ 公式如下：**

$$T_n = 8\Delta t + (n-1) \times 3\Delta t = (3n + 5)\Delta t$$

**3.计算吞吐率 $TP$**
**吞吐率（TP）** 的定义是单位时间内完成的指令数量：

$$TP = \frac{n}{T_n}$$

### 3:计算机图像文件格式
##### 3.1静态图像文件格式
- **JPEG / JPG** (Joint Photographic Experts Group)：应用最广的有损压缩格式，适合照片。
    
- **PNG** (Portable Network Graphics)：支持透明背景的无损压缩格式，常用于网页设计。
    
- **BMP** (Bitmap)：Windows标准位图格式，通常不压缩，文件较大。
    
- **TIFF / TIF** (Tagged Image File Format)：高保真图像格式，常用于印刷和出版。
    
- **PSD**：Adobe Photoshop 的专用分层格式。
    
- **RAW**：数码相机原始数据格式，保留了最多的图像细节
###### 容易混淆的格式

- **GIF (Graphics Interchange Format)**：
    
    - 它在技术上既可以存储**静态**图像（支持256色），也可以存储**短动画**。
        
    - 但在大多数多媒体考试分类中，由于它能承载多帧图像，有时会被归类为简单的动态格式。
        
- **SVG (Scalable Vector Graphics)**：
    
    - 这是一种**矢量**图形格式，虽然是静态的，但它是基于 XML 描述的，与上述位图格式（点阵图）在原理上有本质区别。

##### 3.2动态图像文件格式
AVI、MP4、MPG / MPEG、WMV、MOV、FLV、RMVB
