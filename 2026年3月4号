计算机中的浮点数由三部分组成：符号位S，指数部分E(称为阶码)和尾数部分M。在总长度固定的情况下，增加E的位数、减少M的位数可以：扩大可以表示的数的范围同时降低精度

S(符号位)：专门用来判断数字的正负，其中S=0（+正数），S=1(-负数)(==决定正负==)
E(阶码)：增加阶码位数，能表示更大或更小的数(范围变大)(==决定范围==)
M(尾数)：减少尾数位数，能表示的有效数字变少了(精度降低)(==决定精度==)

公钥(Public Key)和私钥(Private Key)
1.核心概念：钥匙成对出现
公钥和私钥是**成对生成**，它们之间有一种神奇的数学关系：
公钥(**Public Key**):可以公开给任何人，就像你的==信箱投递口==或者==印章的公开样本==
私钥(**Private Key**):必须自己秘密保存，决不能给别人，就像你的信箱钥匙或者手中的实体印章。

铁律：
1：公钥加密的内容，只有对应的私钥能够解开。
2：私钥签名(加密的内容)，只有对应的公钥能验证(解开)。


| 场景   | 目的       | 发送方动作     | 接收方动作     | 钥匙使用情况  |
| ---- | -------- | --------- | --------- | ------- |
| 加密传输 | 保密(防偷看)  | 用对方公钥加密   | 用自己私钥解密   | 公钥锁，私钥开 |
| 数字签名 | 认证(防伪造)  | 用自己私钥签名   | 用对方公钥验签   | 私钥盖，公钥验 |
| 数字证书 | 信任(CA担保) | CA用私钥签名证书 | 用户用CA公钥验证 |         |
