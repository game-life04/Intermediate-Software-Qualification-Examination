### 编译程序和解释程序的区别
| 特性       | 编辑程序 (Editor)                    | 解释程序 (Interpreter)                |
| :------- | :------------------------------- | :-------------------------------- |
| **主要功能** | **编写和修改**源代码                     | **执行**源代码                         |
| **工作阶段** | 编程开发阶段                           | 程序运行阶段                            |
| **处理对象** | 纯文本文件（如 .c, .py, .rb）            | 源代码指令                             |
| **产出结果** | 保存后的代码文件                         | 程序的运行结果                           |
| **典型代表** | VS Code, Notepad, Vim            | Python 解释器，Ruby 解释器               |
| **关系**   | 是编写代码的工具                         | 是运行代码的工具                          |
| 特性       | 编译程序 (Compiler)                  | 解释程序 (Interpreter)                |
| **翻译方式** | **一次性翻译**：将整个源代码程序全部转换成机器语言目标程序。 | **逐行翻译**：读一条源代码，翻译一条，执行一条。        |
| **执行速度** | **快**：因为生成的是直接可执行的机器码。           | **慢**：因为每次运行都需要实时翻译。              |
| **跨平台性** | **差**：生成的目标程序依赖特定操作系统/硬件，需重新编译。  | **好**：只要有对应的解释器，源代码即可在不同平台运行。     |
| **错误提示** | 编译时统一报错，运行前可知大部分语法错误。            | 运行到出错行才报错，便于调试但错误发现较晚。            |
| **典型语言** | **C, C++**, Go, Rust             | **Ruby, Python**, JavaScript, PHP |
| **中间文件** | 会生成独立的目标文件（如 .exe, .o）。          | 通常不生成独立的目标文件，直接执行。                |

### 脚本语言使用场景

1.执行方式：脚本语言通常采用解释方式执行
2.类型系统：脚本语言通常是动态型语言
3.效率对比：脚本语言的开发效率高，但运行效率通常低于编译型语言
4.应用场景：脚本语言常用于Web开发，自动化运维或作为“胶水语言”连接其他组件


### 高级语言源程序

1：高级——>转化中间代码的方式
(1)**后缀式（Postfix Notation）**：
- 又称**逆波兰式**。
- 特点：操作符跟在操作数后面，无需括号即可确定运算顺序，适合栈式计算。
- 例子：`a + b * c` 的后缀式为 `a b c * +`。
(2) **三地址码（Three-Address Code）**：

- 特点：每条指令最多包含三个操作数（两个源操作数，一个目的操作数），形式接近汇编语言但更抽象。
- 常见实现：**四元式**（Quadruples）、三元式（Triples）。
- 例子：`t1 = b * c`, `t2 = a + t1`。
- _注：在某些具体的考试题中，如果选项里没有“三地址码”，**四元式** 也是最常用的具体实形式也可以作为答案。

(3)**树（Tree）**：

- 通常指**语法树**（Syntax Tree）或**抽象语法树**（AST）。
- 特点：用树形结构表示程序的语法结构，直观反映运算的优先级和层次。

为什么要中间代码！
- **便于优化**：在中间代码层可以进行与机器无关的代码优化。
- **便于移植**：编译器前端生成统一的中间代码，后端只需针对不同机器生成目标代码，实现了“一次编译，多处运行”的基础（如 Java 字节码）。
